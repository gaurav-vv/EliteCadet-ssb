import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: {
    default: "SSB Academy — SSB Preparation, AI Feedback & Mentor Guidance",
    template: "%s · SSB Academy",
  },
  description:
    "Prepare for the Services Selection Board with structured practice, AI-assisted feedback, mentor guidance and academy visibility.",
};

export default function RootLayout({ children }: LayoutProps<"/">) {
  return (
    <html
      lang="en"
      className={`${geistSans.variable} ${geistMono.variable} h-full antialiased`}
    >
      <body className="min-h-full flex flex-col">
        <div className="ambient-wash" aria-hidden="true" />
        {children}
      </body>
    </html>
  );
}
